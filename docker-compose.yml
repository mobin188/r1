services:
  web:
    build: .
    ports:
      - "5000:5000"
    env_file:
      - .env
    environment:
      - FLASK_DEBUG=1
      - FLASK_APP=app:create_app   # Flask CLI understands the factory
    volumes:
      - .:/app
    command: flask run --host=0.0.0.0 --port=5000 --reload
